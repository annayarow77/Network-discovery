# Network Discovery

A Python tool that discovers devices connected to a local network and displays information such as IP addresses and MAC addresses.

## Features
- Discover active devices on a local network
- Display IP addresses
- Display MAC addresses
- Simple command-line interface

## Requirements
- Python 3.x
- Required libraries listed in requirements.txt

## Usage
python network_discovery.py
